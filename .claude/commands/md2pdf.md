# /md2pdf - 마크다운을 PDF로 변환

한글 폰트를 지원하는 마크다운 PDF 변환 도구입니다.

## 중요: 실행 전 반드시 수행할 단계

**md2pdf 실행 전에 반드시 아래 단계를 순서대로 수행하세요:**

### Step 1: 설치 여부 확인
```bash
which dahtmad-md2pdf
```

### Step 2-A: 이미 설치된 경우 → 도구 + 커맨드 파일 업데이트 (필수!)

**반드시 GitHub에서 최신 버전인지 확인하고, 도구와 커맨드 파일 모두 업데이트하세요:**

```bash
TOOL_PATH=$(dirname $(dirname $(which dahtmad-md2pdf))) && cd $TOOL_PATH && git fetch origin && LOCAL=$(git rev-parse HEAD) && REMOTE=$(git rev-parse origin/master) && if [ "$LOCAL" != "$REMOTE" ]; then echo "🔄 새 버전 발견, 업데이트 중..." && git pull origin master && npm install && npm run build && cp .claude/commands/md2pdf.md $OLDPWD/.claude/commands/md2pdf.md && echo "✅ 도구 및 커맨드 파일 업데이트 완료!"; else echo "✅ 이미 최신 버전입니다."; fi
```

### Step 2-B: 설치되지 않은 경우 → 자동 설치
```bash
git clone https://github.com/daht-mad/md2pdf.git /tmp/md2pdf && cd /tmp/md2pdf && npm install && npm run build && npm link
```

### Step 3: 파일 찾기

Glob 도구로 마크다운 파일을 검색합니다:
```
**/*.md
```

사용자가 입력한 파일명과 일치하는 파일을 찾습니다:
- 대소문자 구분 없이, 부분 일치로 검색
- 여러 개 발견되면 목록을 보여주고 선택하게 합니다
- 정확히 일치하는 파일이 없지만 유사한 파일이 있으면 제안

### Step 4: 도구 실행
```bash
dahtmad-md2pdf "<확정된-파일명>"
```

**중요:** 파일 경로에 공백이 있을 수 있으므로 반드시 따옴표로 감싸세요.

### Step 5: 결과 확인

- 성공 시: 동일한 위치에 `.pdf` 파일이 생성됩니다
- 실패 시: 에러 메시지를 확인하고 해결 방법을 안내합니다

## 사용법

```
/md2pdf <마크다운파일>
```

## 맥락 확인 (Step 0)

**중요: 도구 실행 전에 필요한 정보가 충분한지 먼저 확인하세요.**

사용자가 `/md2pdf`만 입력하거나 파일명이 없는 경우:
1. "어떤 마크다운 파일을 PDF로 변환할까요?"
2. "예: `/md2pdf README.md` 또는 `/md2pdf 회의록.md`"

**파일명이 제공된 경우에만** 아래 실행 단계로 진행합니다.

## 기능

- 한글 폰트 지원
- 깔끔한 스타일링
- 코드 블록 구문 강조
- 자동 파일 검색

## 예시

```
/md2pdf README.md
/md2pdf ./docs/guide.md
/md2pdf 회의록.md
```

출력:
```
📄 마크다운 파일 변환 중: README.md
✅ 변환 완료!
   출력 파일: README.pdf
```

## 에러 처리

- **"command not found"**: Step 2-B 실행
- **"파일을 찾을 수 없습니다"**: 파일 경로 확인
- **설치 실패**: Node.js/npm 확인 안내
